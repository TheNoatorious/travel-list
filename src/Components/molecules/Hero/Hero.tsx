import "./Hero.css";

const Hero = (): React.JSX.Element => {
    return (
        <div className="hero">
            <h1 className="hero__title">Travel List 🎒</h1>
        </div>
    );
};

export default Hero;
