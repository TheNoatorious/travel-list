import Button from "../../atoms/Button/Button";

const Navigation = () => {
    return (
        <>
            <Button label={"Add"} disabled={false} />
        </>
    );
};

export default Navigation;
